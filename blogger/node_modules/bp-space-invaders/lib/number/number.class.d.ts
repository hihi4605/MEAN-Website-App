import { GameService } from '../service/game.service';
import { AssetClass } from '../asset/asset.class';
import { BoundarySetupModel } from '../model/boundary-set.model';
export declare class NumberClass extends AssetClass {
    private color;
    private numbers;
    constructor(gameService: GameService, ctx: CanvasRenderingContext2D, boundarySetup: BoundarySetupModel, color: number);
    protected spawn(): void;
    protected draw(): void;
    private drawNumber;
    private padPoints;
    setNumber(inputNumber: number, isPadNumber?: boolean): void;
    animate(): void;
}
