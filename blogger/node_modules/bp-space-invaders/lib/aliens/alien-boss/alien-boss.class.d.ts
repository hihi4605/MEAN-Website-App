import { GameService } from '../../service/game.service';
import { AlienClass } from '../alien/alien.class';
import { BoundarySetupModel } from '../../model/boundary-set.model';
export declare abstract class AlienBossClass extends AlienClass {
    private directionLeft;
    private moves;
    constructor(gameService: GameService, ctx: CanvasRenderingContext2D, boundarySetup: BoundarySetupModel, guid: number, points: number);
    private determineBossDirection;
    positionAlienBoss(): void;
    animate(now?: number): void;
}
